APP_FQDN=${SEMAPHORE_EXTERNAL_HOSTNAME:-{{ semaphore_external_hostname }}}
APP_PORT=${SEMAPHORE_SERVICE_PORT:-{{ semaphore_service_port }}}
APP_BACKEND_IP=${SEMAPHORE_APP_BACKEND_IP:-{{ service_ip }}}
SEMAPHORE_WEBHOOK_URL=${SEMAPHORE_WEBHOOK_URL:-{{ semaphore_webhook_url }}}
